{:map {:comma? false}
 ;; Stop it from wrapping comments. Should have done this a while ago
 :comment {:wrap? false}
 ;; Set as :fn so it goes
 ;; (testing "test name"
 ;;   (is ...
 :fn-map {"testing" :fn
          "reg-sub" :arg1
          "reg-sub-raw" :arg1
          "reg-event-fx" :arg1
          }
 ;; Ask zprint to respect blank lines, useful for large masses of declarative code that needs visual separation.
 :style :respect-bl
 ;; If all the elements of a vector fit on one line, do that, otherwise put each element on its own line. 
 :vector {:wrap? false}
}
